name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.23'
          cache: true

      - uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Download dependencies
        run: go mod download

      - name: Format check
        run: |
          test -z "$(gofmt -l .)"

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build -v ./...

      - name: Platform matrix build
        shell: pwsh
        run: ./scripts/check-platform-matrix.ps1

      - name: Test
        run: go test -v -count=1 ./...

      - name: CLI smoke
        run: |
          go run . version
          go run . backup -c config.yaml --dry-run
          go run . backup -c config.yaml --job mysql-game --dry-run
          go run . install mysqldump --dry-run
          go run . update --tag v0.1.0 --dry-run
          go run . update --tag v0.1.0 --check
          go run . cron "0 2 * * *" -c config.yaml --dry-run
          go run . cron remove --dry-run

  install-script-smoke:
    name: Native Smoke (${{ matrix.name }})
    runs-on: ${{ matrix.runs_on }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-amd64
            runs_on: ubuntu-latest
            ext: ''
          - name: linux-arm64
            runs_on: ubuntu-24.04-arm
            ext: ''
          - name: darwin-amd64
            runs_on: macos-15-intel
            ext: ''
          - name: darwin-arm64
            runs_on: macos-latest
            ext: ''
          - name: windows-amd64
            runs_on: windows-latest
            ext: '.exe'
          - name: windows-arm64
            runs_on: windows-11-arm
            ext: '.exe'
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.23'
          cache: true

      - uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Build local binary (Unix)
        if: runner.os != 'Windows'
        run: go build -o dbbackup233-test .

      - name: Build local binary (Windows)
        if: runner.os == 'Windows'
        run: go build -o dbbackup233-test.exe .

      - name: Script syntax smoke (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          test -f scripts/install.sh
          test -f scripts/install.ps1
          ./dbbackup233-test version

      - name: Script syntax smoke (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Test-Path scripts/install.sh
          Test-Path scripts/install.ps1
          .\dbbackup233-test.exe version

      - name: CLI smoke (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          ./dbbackup233-test backup -c config.yaml --dry-run
          ./dbbackup233-test backup -c config.yaml --job mysql-game --dry-run
          ./dbbackup233-test prune -c config.yaml --dry-run
          ./dbbackup233-test update --tag v0.1.0 --dry-run
          ./dbbackup233-test update --tag v0.1.0 --check
          ./dbbackup233-test cron "0 2 * * *" -c config.yaml --dry-run
          ./dbbackup233-test cron remove --dry-run

      - name: CLI smoke (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          .\dbbackup233-test.exe backup -c config.yaml --dry-run
          .\dbbackup233-test.exe backup -c config.yaml --job mysql-game --dry-run
          .\dbbackup233-test.exe prune -c config.yaml --dry-run
          .\dbbackup233-test.exe update --tag v0.1.0 --dry-run
          .\dbbackup233-test.exe update --tag v0.1.0 --check
          .\dbbackup233-test.exe cron "0 2 * * *" -c config.yaml --dry-run
          .\dbbackup233-test.exe cron remove --dry-run

  build-matrix:
    name: Build (${{ matrix.goos }}/${{ matrix.goarch }})
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            ext: ''
          - goos: linux
            goarch: arm64
            ext: ''
          - goos: darwin
            goarch: amd64
            ext: ''
          - goos: darwin
            goarch: arm64
            ext: ''
          - goos: windows
            goarch: amd64
            ext: '.exe'
          - goos: windows
            goarch: arm64
            ext: '.exe'
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.23'
          cache: true

      - uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Cross-compile
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          mkdir -p dist
          go build -ldflags="-s -w -X github.com/neko233-com/dbbackup233/cmd.version=${{ github.sha }}" -o "dist/dbbackup233-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }}" .
